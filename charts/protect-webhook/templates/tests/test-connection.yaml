apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "protect-webhook.fullname" . }}-test-connection"
  labels:
    {{- include "protect-webhook.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "protect-webhook.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
